#include<iostream>

class Solution {
public:
    int majorityElement(vector<int>& nums) {
        vector<int> tmp(nums.size());
        return 0;
    }

    void merge_sort(vector<int>& nums, vector<int>& tmp) {

    }


};

int main() {
    return 0;
}
